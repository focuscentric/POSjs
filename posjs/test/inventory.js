var should = require("should");

describe("Inventory", function () {
    it("be a first failing test", function () {
        var a = 5;
        var b = 5;

        a.should.equal(b);
    })
});